services:
  postgres:
    image: postgres:16-alpine
    container_name: selfhost-bench-postgres
    environment:
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: selfhost-bench
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d selfhost-bench"]
      interval: 3s
      timeout: 3s
      retries: 20

  mosquitto:
    image: eclipse-mosquitto:2
    container_name: selfhost-bench-mosquitto
    ports:
      - "${MOSQUITTO_PORT:-1883}:1883"

  emqx:
    image: emqx/emqx:latest
    container_name: selfhost-bench-emqx
    ports:
      - "${EMQX_PORT:-1886}:1883"

  aapije:
    build:
      context: ../..
      dockerfile: bench/docker/Dockerfile.aapije
    container_name: selfhost-bench-aapije
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      CONFIG_FILENAME: aapije.conf.yaml
    volumes:
      - ./config:/etc/selfhost:ro
    ports:
      - "8080:80"
