// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: timeseries.sql

package postgres

import (
	"context"
	"database/sql"

	"github.com/google/uuid"
	"github.com/lib/pq"
)

const createTimeseries = `-- name: CreateTimeseries :one
WITH t AS (
	INSERT INTO timeseries(
		thing_uuid,
		name,
		si_unit,
		lower_bound,
		upper_bound,
		created_by,
		tags
	) VALUES (
		NULLIF($1::uuid, '00000000-0000-0000-0000-000000000000'::uuid),
		$2,
		$3,
		$4,
		$5,
		$6,
		$7
	) RETURNING uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags
), grp AS (
	SELECT groups.uuid
	FROM groups, user_groups
	WHERE user_groups.group_uuid = groups.uuid
	AND user_groups.user_uuid = (SELECT created_by FROM t)
	AND groups.uuid = (
		SELECT users.uuid
		FROM users
		WHERE users.name = groups.name
	)
	LIMIT 1
), grp_policies AS (
	INSERT INTO group_policies(group_uuid, priority, effect, action, resource)
	VALUES (
		(SELECT uuid FROM grp), 0, 'allow', 'create','timeseries/'||(SELECT uuid FROM t)||'/%'
	), (
		(SELECT uuid FROM grp), 0, 'allow', 'read','timeseries/'||(SELECT uuid FROM t)||'/%'
	), (
		(SELECT uuid FROM grp), 0, 'allow', 'update','timeseries/'||(SELECT uuid FROM t)||'/%'
	), (
		(SELECT uuid FROM grp), 0, 'allow', 'delete','timeseries/'||(SELECT uuid FROM t)||'/%'
	)
)
SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags
FROM t LIMIT 1
`

type CreateTimeseriesParams struct {
	ThingUuid  uuid.UUID
	Name       string
	SiUnit     string
	LowerBound sql.NullFloat64
	UpperBound sql.NullFloat64
	CreatedBy  uuid.NullUUID
	Tags       []string
}

type CreateTimeseriesRow struct {
	Uuid       uuid.UUID
	ThingUuid  uuid.NullUUID
	Name       string
	SiUnit     string
	LowerBound sql.NullFloat64
	UpperBound sql.NullFloat64
	CreatedBy  uuid.NullUUID
	Tags       []string
}

func (q *Queries) CreateTimeseries(ctx context.Context, arg CreateTimeseriesParams) (CreateTimeseriesRow, error) {
	row := q.queryRow(ctx, q.createTimeseriesStmt, createTimeseries,
		arg.ThingUuid,
		arg.Name,
		arg.SiUnit,
		arg.LowerBound,
		arg.UpperBound,
		arg.CreatedBy,
		pq.Array(arg.Tags),
	)
	var i CreateTimeseriesRow
	err := row.Scan(
		&i.Uuid,
		&i.ThingUuid,
		&i.Name,
		&i.SiUnit,
		&i.LowerBound,
		&i.UpperBound,
		&i.CreatedBy,
		pq.Array(&i.Tags),
	)
	return i, err
}

const deleteTimeseries = `-- name: DeleteTimeseries :execrows
DELETE FROM timeseries
WHERE uuid = $1
`

func (q *Queries) DeleteTimeseries(ctx context.Context, argUuid uuid.UUID) (int64, error) {
	result, err := q.exec(ctx, q.deleteTimeseriesStmt, deleteTimeseries, argUuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const existsTimeseries = `-- name: ExistsTimeseries :one
SELECT COUNT(*) AS count
FROM timeseries
WHERE timeseries.uuid = $1
`

func (q *Queries) ExistsTimeseries(ctx context.Context, argUuid uuid.UUID) (int64, error) {
	row := q.queryRow(ctx, q.existsTimeseriesStmt, existsTimeseries, argUuid)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const findTimeseries = `-- name: FindTimeseries :many
WITH usr AS (
	SELECT user_tokens.user_uuid AS uuid
	FROM user_tokens
	WHERE user_tokens.token_hash = sha256($1)
	LIMIT 1
), permitted_timeseries AS (
	SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags
	FROM timeseries
	WHERE EXISTS (
		SELECT 1
		FROM usr
		INNER JOIN user_groups ON user_groups.user_uuid = usr.uuid
		INNER JOIN group_policies ON group_policies.group_uuid = user_groups.group_uuid
		WHERE group_policies.action = 'read'
		AND group_policies.effect = 'allow'
		AND ('timeseries/'||timeseries.uuid) LIKE group_policies.resource
	)
	AND NOT EXISTS (
		SELECT 1
		FROM usr
		INNER JOIN user_groups ON user_groups.user_uuid = usr.uuid
		INNER JOIN group_policies ON group_policies.group_uuid = user_groups.group_uuid
		WHERE group_policies.action = 'read'
		AND group_policies.effect = 'deny'
		AND ('timeseries/'||timeseries.uuid) LIKE group_policies.resource
	)
	ORDER BY name
	LIMIT $3::BIGINT
	OFFSET $2::BIGINT
)
SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags
FROM permitted_timeseries
`

type FindTimeseriesParams struct {
	Token     []byte
	ArgOffset int64
	ArgLimit  int64
}

type FindTimeseriesRow struct {
	Uuid       uuid.UUID
	ThingUuid  uuid.NullUUID
	Name       string
	SiUnit     string
	LowerBound sql.NullFloat64
	UpperBound sql.NullFloat64
	CreatedBy  uuid.NullUUID
	Tags       []string
}

func (q *Queries) FindTimeseries(ctx context.Context, arg FindTimeseriesParams) ([]FindTimeseriesRow, error) {
	rows, err := q.query(ctx, q.findTimeseriesStmt, findTimeseries, arg.Token, arg.ArgOffset, arg.ArgLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FindTimeseriesRow{}
	for rows.Next() {
		var i FindTimeseriesRow
		if err := rows.Scan(
			&i.Uuid,
			&i.ThingUuid,
			&i.Name,
			&i.SiUnit,
			&i.LowerBound,
			&i.UpperBound,
			&i.CreatedBy,
			pq.Array(&i.Tags),
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const findTimeseriesByTags = `-- name: FindTimeseriesByTags :many
WITH usr AS (
	SELECT user_tokens.user_uuid AS uuid
	FROM user_tokens
	WHERE user_tokens.token_hash = sha256($1)
	LIMIT 1
), permitted_timeseries AS (
	SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags
	FROM timeseries
	WHERE $2 && timeseries.tags
	AND EXISTS (
		SELECT 1
		FROM usr
		INNER JOIN user_groups ON user_groups.user_uuid = usr.uuid
		INNER JOIN group_policies ON group_policies.group_uuid = user_groups.group_uuid
		WHERE group_policies.action = 'read'
		AND group_policies.effect = 'allow'
		AND ('timeseries/'||timeseries.uuid) LIKE group_policies.resource
	)
	AND NOT EXISTS (
		SELECT 1
		FROM usr
		INNER JOIN user_groups ON user_groups.user_uuid = usr.uuid
		INNER JOIN group_policies ON group_policies.group_uuid = user_groups.group_uuid
		WHERE group_policies.action = 'read'
		AND group_policies.effect = 'deny'
		AND ('timeseries/'||timeseries.uuid) LIKE group_policies.resource
	)
	ORDER BY name
	LIMIT $4::BIGINT
	OFFSET $3::BIGINT
)
SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags
FROM permitted_timeseries
`

type FindTimeseriesByTagsParams struct {
	Token     []byte
	Tags      []string
	ArgOffset int64
	ArgLimit  int64
}

type FindTimeseriesByTagsRow struct {
	Uuid       uuid.UUID
	ThingUuid  uuid.NullUUID
	Name       string
	SiUnit     string
	LowerBound sql.NullFloat64
	UpperBound sql.NullFloat64
	CreatedBy  uuid.NullUUID
	Tags       []string
}

func (q *Queries) FindTimeseriesByTags(ctx context.Context, arg FindTimeseriesByTagsParams) ([]FindTimeseriesByTagsRow, error) {
	rows, err := q.query(ctx, q.findTimeseriesByTagsStmt, findTimeseriesByTags,
		arg.Token,
		pq.Array(arg.Tags),
		arg.ArgOffset,
		arg.ArgLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FindTimeseriesByTagsRow{}
	for rows.Next() {
		var i FindTimeseriesByTagsRow
		if err := rows.Scan(
			&i.Uuid,
			&i.ThingUuid,
			&i.Name,
			&i.SiUnit,
			&i.LowerBound,
			&i.UpperBound,
			&i.CreatedBy,
			pq.Array(&i.Tags),
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const findTimeseriesByThing = `-- name: FindTimeseriesByThing :many
SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags FROM timeseries
WHERE $1 = timeseries.thing_uuid
ORDER BY name
`

func (q *Queries) FindTimeseriesByThing(ctx context.Context, thingUuid uuid.NullUUID) ([]Timeseries, error) {
	rows, err := q.query(ctx, q.findTimeseriesByThingStmt, findTimeseriesByThing, thingUuid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Timeseries{}
	for rows.Next() {
		var i Timeseries
		if err := rows.Scan(
			&i.Uuid,
			&i.ThingUuid,
			&i.Name,
			&i.SiUnit,
			&i.LowerBound,
			&i.UpperBound,
			&i.CreatedBy,
			pq.Array(&i.Tags),
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const findTimeseriesByUUID = `-- name: FindTimeseriesByUUID :one
SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags FROM timeseries
WHERE $1 = timeseries.uuid
LIMIT 1
`

func (q *Queries) FindTimeseriesByUUID(ctx context.Context, tsUuid uuid.UUID) (Timeseries, error) {
	row := q.queryRow(ctx, q.findTimeseriesByUUIDStmt, findTimeseriesByUUID, tsUuid)
	var i Timeseries
	err := row.Scan(
		&i.Uuid,
		&i.ThingUuid,
		&i.Name,
		&i.SiUnit,
		&i.LowerBound,
		&i.UpperBound,
		&i.CreatedBy,
		pq.Array(&i.Tags),
	)
	return i, err
}

const getTimeseriesByUUID = `-- name: GetTimeseriesByUUID :one
SELECT uuid, thing_uuid, name, si_unit, lower_bound, upper_bound, created_by, tags FROM timeseries
WHERE uuid = $1
LIMIT 1
`

func (q *Queries) GetTimeseriesByUUID(ctx context.Context, argUuid uuid.UUID) (Timeseries, error) {
	row := q.queryRow(ctx, q.getTimeseriesByUUIDStmt, getTimeseriesByUUID, argUuid)
	var i Timeseries
	err := row.Scan(
		&i.Uuid,
		&i.ThingUuid,
		&i.Name,
		&i.SiUnit,
		&i.LowerBound,
		&i.UpperBound,
		&i.CreatedBy,
		pq.Array(&i.Tags),
	)
	return i, err
}

const getUnitFromTimeseries = `-- name: GetUnitFromTimeseries :one
SELECT si_unit FROM timeseries
WHERE uuid = $1
LIMIT 1
`

func (q *Queries) GetUnitFromTimeseries(ctx context.Context, argUuid uuid.UUID) (string, error) {
	row := q.queryRow(ctx, q.getUnitFromTimeseriesStmt, getUnitFromTimeseries, argUuid)
	var si_unit string
	err := row.Scan(&si_unit)
	return si_unit, err
}

const setTimeseriesLowerBound = `-- name: SetTimeseriesLowerBound :execrows
UPDATE timeseries
SET lower_bound = $1
WHERE timeseries.uuid = $2
`

type SetTimeseriesLowerBoundParams struct {
	LowerBound sql.NullFloat64
	Uuid       uuid.UUID
}

func (q *Queries) SetTimeseriesLowerBound(ctx context.Context, arg SetTimeseriesLowerBoundParams) (int64, error) {
	result, err := q.exec(ctx, q.setTimeseriesLowerBoundStmt, setTimeseriesLowerBound, arg.LowerBound, arg.Uuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const setTimeseriesName = `-- name: SetTimeseriesName :execrows
UPDATE timeseries
SET name = $1
WHERE timeseries.uuid = $2
`

type SetTimeseriesNameParams struct {
	Name string
	Uuid uuid.UUID
}

func (q *Queries) SetTimeseriesName(ctx context.Context, arg SetTimeseriesNameParams) (int64, error) {
	result, err := q.exec(ctx, q.setTimeseriesNameStmt, setTimeseriesName, arg.Name, arg.Uuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const setTimeseriesSiUnit = `-- name: SetTimeseriesSiUnit :execrows
UPDATE timeseries
SET si_unit = $1
WHERE timeseries.uuid = $2
`

type SetTimeseriesSiUnitParams struct {
	SiUnit string
	Uuid   uuid.UUID
}

func (q *Queries) SetTimeseriesSiUnit(ctx context.Context, arg SetTimeseriesSiUnitParams) (int64, error) {
	result, err := q.exec(ctx, q.setTimeseriesSiUnitStmt, setTimeseriesSiUnit, arg.SiUnit, arg.Uuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const setTimeseriesTags = `-- name: SetTimeseriesTags :execrows
UPDATE timeseries
SET tags = $1
WHERE timeseries.uuid = $2
`

type SetTimeseriesTagsParams struct {
	Tags []string
	Uuid uuid.UUID
}

func (q *Queries) SetTimeseriesTags(ctx context.Context, arg SetTimeseriesTagsParams) (int64, error) {
	result, err := q.exec(ctx, q.setTimeseriesTagsStmt, setTimeseriesTags, pq.Array(arg.Tags), arg.Uuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const setTimeseriesThing = `-- name: SetTimeseriesThing :execrows
UPDATE timeseries
SET thing_uuid = $1
WHERE timeseries.uuid = $2
`

type SetTimeseriesThingParams struct {
	ThingUuid uuid.NullUUID
	Uuid      uuid.UUID
}

func (q *Queries) SetTimeseriesThing(ctx context.Context, arg SetTimeseriesThingParams) (int64, error) {
	result, err := q.exec(ctx, q.setTimeseriesThingStmt, setTimeseriesThing, arg.ThingUuid, arg.Uuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const setTimeseriesUpperBound = `-- name: SetTimeseriesUpperBound :execrows
UPDATE timeseries
SET upper_bound = $1
WHERE timeseries.uuid = $2
`

type SetTimeseriesUpperBoundParams struct {
	UpperBound sql.NullFloat64
	Uuid       uuid.UUID
}

func (q *Queries) SetTimeseriesUpperBound(ctx context.Context, arg SetTimeseriesUpperBoundParams) (int64, error) {
	result, err := q.exec(ctx, q.setTimeseriesUpperBoundStmt, setTimeseriesUpperBound, arg.UpperBound, arg.Uuid)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
